// Globals will contain useful globals used in many files across the app
#pragma once

#include <opencv2/opencv.hpp>

// Input image dimensions
int const IMAGE_WIDTH = 640;
int const IMAGE_HEIGHT = 480;

//
//// Graphics Constants
//
Scalar const RED = Scalar(0,0,255);
Scalar const GREEN = Scalar(0,255,0);
Scalar const ORANGE = Scalar(0,80,200);
Scalar const BLUE = Scalar(255,0,0);
Scalar const BLACK = Scalar(0,0,0);
Scalar const WHITE = Scalar(255,255,255);
Scalar const GRAY = Scalar(127,127,127);

